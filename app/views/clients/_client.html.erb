<div id="<%= dom_id client %>">
  <tr>
    <th scope="row"><%= client.id %></th>
    <td><%= client.name %></td>
    <td><%= client.age %></td>
    <td><%= client.movies.last %></td>

    <td>
      <%= link_to client_path(client), class:'btn btn-primary' do %>
        <i class="fa-regular fa-eye"></i>
      <% end %>
    </td>
   
    <td>
      <%= link_to edit_client_path(client), class:'btn btn-success' do %>
        <i class="fa-solid fa-pen-to-square"></i>
      <% end %>
    </td>
    <td>
      <%= link_to client_path(client.id), class:'btn btn-danger', data: {
        turbo_method: :delete,
        turbo_confirm: '¿Seguro desea Eliminar este Cliente?'
    } do %>
      <i class="fa-solid fa-trash"></i>
  <% end %></td>

    
   
    
  </tr>
</div>
